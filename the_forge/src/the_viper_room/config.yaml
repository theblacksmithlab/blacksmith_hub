server:
  host: "0.0.0.0"
  port: 3001

# TLS section removed - nginx handles TLS termination
# This service runs as HTTP only, nginx proxies HTTPS → HTTP

cors:
  allowed_origins:
    - "https://0xthecableguy.github.io"
    - "http://localhost:5173"
    - "http://localhost:3000"
